<!DOCTYPE html>
<html>
<head>
  <title>Recipe App</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>
  
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</head>
<body class="bg-olive">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="#">Recip<span class="text-uppercase">all</span>🥑E</a>
      <% if user_signed_in? %>
        <%= link_to 'Generate Recipe', new_recipe_path, class: "nav-link" %>
        <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-danger' %>
      <% else %>
      <%= link_to 'Sign Up', '#', class: 'btn btn-primary', data: { bs_toggle: "modal", bs_target: "#sign-up-modal" } %>
      <%= link_to 'Login', '#', class: 'btn btn-secondary', data: { bs_toggle: "modal", bs_target: "#login-modal" } %>
      <% end %>
    </div>
  </nav>

  <header class="jumbotron jumbotron-fluid text-center bg-avocado" id="changing-banner">
    <div class="container">
      <h1 class="display-4">Welcome to the Recipe App</h1>
      <p class="lead">Discover and share your favorite recipes.</p>
    </div>
  </header>

  <footer class="bg-avocado py-3">
    <div class="container text-center">
      <p>&copy; 2023 Recipe App. All Rights Reserved.</p>
      <p>Contact: <a href="mailto:info@example.com">info@example.com</a></p>
      <div class="social-links">
        <a href="https://www.facebook.com" target="_blank">
          <img src="path-to-facebook-logo" alt="Facebook" width="32" height="32">
        </a>
        <a href="https://www.twitter.com" target="_blank">
          <img src="path-to-twitter-logo" alt="Twitter" width="32" height="32">
        </a>
        <a href="https://www.instagram.com" target="_blank">
          <img src="path-to-instagram-logo" alt="Instagram" width="32" height="32">
        </a>
      </div>
    </div>
  </footer>

  <%= render "devise/registrations/sign_up_modal", resource: User.new, resource_name: :user, devise_mapping: Devise.mappings[:user] %>
  <%= render "devise/sessions/login_modal", resource: User.new, resource_name: :user, devise_mapping: Devise.mappings[:user] %>

  <script>
    const images = [
      'url-to-image-1',
      'url-to-image-2',
      'url-to-image-3',
    ];

    let currentImageIndex = 0;
    const changingBanner = document.getElementById("changing-banner");

    function changeBannerImage() {
      changingBanner.style.backgroundImage = `url(${images[currentImageIndex]})`;
      currentImageIndex++;
      if (currentImageIndex >= images.length) {
        currentImageIndex = 0;
      }
    }

    setInterval(changeBannerImage, 5000); // Change banner image every 5 seconds.
  </script>

  <style>
    .bg-avocado {
      background-color: #568203;
    }

    .bg-olive {
      background-color: #808000;
    }

    #changing-banner {
      background-position: center;
      background-repeat: no-repeat;
      background-size: cover;
    }

    .social-links a {
      margin: 0 5px;
    }
  </style>
</body>
</html>
